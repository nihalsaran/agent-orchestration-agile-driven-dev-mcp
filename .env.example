# Point to a custom Codex CLI binary if it's not on PATH
# CODEX_COMMAND="/usr/local/bin/codex"

# Add extra arguments before `mcp-server`
# CODEX_MCP_ARGS="--profile default --startup-timeout 120"

# Provide additional environment variables to the Codex process (JSON object)
# CODEX_MCP_ENV='{"OPENAI_API_KEY":"sk-..."}'

# Run Codex from a specific working directory
# CODEX_MCP_CWD="/path/to/project"
